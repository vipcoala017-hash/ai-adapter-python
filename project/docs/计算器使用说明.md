# 计算器使用说明

## 功能

- 支持加、减、乘、除
- 支持小数、括号和连续计算
- 支持键盘输入、回退、清空和回车计算

## 入口

在 `project/` 目录下执行：

```bash
python calculator.py
```

## 交互说明

- `C`：清空表达式
- `⌫`：删除最后一个字符
- `=`：计算结果
- `Esc`：清空
- `Enter`：计算

## 设计说明

计算逻辑与 UI 分离，表达式通过安全解析器计算，不直接执行任意代码。
