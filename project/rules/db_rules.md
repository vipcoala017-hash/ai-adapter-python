---
name: "DB与数据规则"
id: db_rules
category: rules
---

## DB与数据规则

- DB 设计位于程序实现前，字段、状态、读写边界和存储边界必须明确。
- 静态数据、动态数据、本地存储、云存储、缓存和同步边界不能混写。
- 所有正式游戏数值从 JSON 数据读取。
- 每个数据类使用独立 JSON 文件。
- 新增数值类时同步补文档、JSON、字段说明和读取逻辑。
- 数值变更必须同步更新文档、JSON 和测试断言。